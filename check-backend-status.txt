The backend is still down! We need to check what happened with the rebuild.

Run these commands to diagnose:

1. Check container status:
ssh root@5.78.147.82 "cd /var/www/Haveninstitute && docker compose ps"

2. Check backend logs:
ssh root@5.78.147.82 "cd /var/www/Haveninstitute && docker compose logs backend --tail=50"

3. Check if backend is actually running:
ssh root@5.78.147.82 "cd /var/www/Haveninstitute && docker compose exec backend ps aux"

4. Try manual rebuild if needed:
ssh root@5.78.147.82 "cd /var/www/Haveninstitute && docker compose down && docker compose up -d --build"

The 502 error means the backend service is not responding on port 3001.
